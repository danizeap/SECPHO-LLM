# Tasks

## Change

sidebar-declutter

## Implementation

- [x] Confirm scope (Owner Option B: explainers + scoring blurb out; Admin → bottom ⚙).
- [x] Remove the five side-block cards; add the bottom `.side-foot` footer (⚙ Admin + Sign out).
- [x] CSS swap (`.side-block` → `.side-foot`), i18n `admin_link` added, dead `block_*` keys removed.
- [x] Verification (AST + escape, esprima JS, full suite, served-page structure check).
- [x] Docs/specs (bilingual-ui delta).
- [ ] Owner live glance: sidebar is clean, ⚙ Admin works, conversations still listed.
